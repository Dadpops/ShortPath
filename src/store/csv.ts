// CSV import/export via PapaParse.
// Phase 1: parse CSV into Entry objects, validate required columns, merge into store.
// Export: serialize store entries back to the same column schema (lossless round-trip).
// Required CSV columns: vertical, title, type. Optional: body, link, tags.

export {};
