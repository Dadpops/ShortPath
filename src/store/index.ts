// JSON store: read/write store.json in Electron's userData directory.
// Phase 1: open store on app start, create default structure on first run,
// handle version migrations, expose load/save to IPC handlers.

export {};
