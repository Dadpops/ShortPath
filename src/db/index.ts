// Database layer: SQLite via better-sqlite3, FTS5 for full-text search.
// Phase 1: schema creation, migrations, open/close lifecycle.
// Phase 2: FTS5 search queries with per-vertical hit counts.

export {};
