// Keyboard navigation for the result list.
// Phase 4: arrow keys move focus through results, Enter copies focused entry,
// Esc hides the window. All keyboard handling lives here, not scattered in components.

export {};
